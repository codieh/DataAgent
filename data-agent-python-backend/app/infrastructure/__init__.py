"""基础设施层（Infrastructure）。

本包封装系统与外部环境的交互细节，是「领域层」与「外部资源」之间的适配器层，
向上层提供与具体实现无关的抽象接口。主要包含以下子模块：

- ``datasource``：业务数据源适配器（如产品数据库只读访问、SQL 安全策略）。
- ``llm``：大模型（LLM）供应商适配器，屏蔽不同厂商 SDK 的差异。
- ``persistence``：持久化适配器，基于 SQLAlchemy 维护 SQLite/其他数据库中的
  对话、运行、记忆、产物等聚合数据，并对外暴露仓储（Repository）门面。

设计要点：基础设施层只依赖配置（``app.config``）与领域层定义的错误/契约，
不反向依赖应用层，从而保证依赖方向单向、可测试、可替换。
"""

