"""可重复的电商演示数据生成器。

提供“确定性”的演示数据（固定随机种子），便于在本地/演示环境复现一致的
商品、用户、订单、退款与促销数据。流程为：生成维度数据 → 分批生成订单事实
→ 写入 MySQL → 完整性校验。各子模块职责：
- ``catalog``：类目/渠道/品牌等静态取值；
- ``generator``：基于配置的确定性数据生成逻辑；
- ``loader``：把生成的数据写入数据库；
- ``validate``：校验数据一致性与完整性；
- ``presets``：规模预设（small/medium/large）；
- ``generate``：命令行入口。
"""
