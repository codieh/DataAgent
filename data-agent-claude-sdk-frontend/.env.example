# 留空表示走当前站点 /api 代理（开发时用 Vite 代理到 localhost:8001）。
# 桌面端连接独立部署的后端时，填完整地址，例如 http://localhost:8001
VITE_BACKEND_URL=

# 前端注入的身份头，后端据此做租户/用户隔离。
# 注意：生产环境应由认证网关签发，不要把用户自填 Header 当成鉴权。
VITE_TENANT_ID=local-tenant
VITE_USER_ID=local-user
